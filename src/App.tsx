import "./App.css";
import { HeartButton } from "./HeartButton";

const App = () => {
  return (
    <div class="w-screen h-screen flex flex-wrap items-center justify-center bg-black">
      <HeartButton />
    </div>
  );
};

export default App;
